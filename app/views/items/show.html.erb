 
   <div class="height shadow d-flex justify-content-center align-items-center">    
    <div class="card p-3">
        <div class="d-flex justify-content-between align-items-center ">
            <div class="mt-2">
                <div class="mt-5">
                    <h5 class="text-uppercase mb-0"></h5>
                    <h1 class="main-heading mt-0"><%= @item.name %></h1>
                    <h1 class="main-heading mt-0">$ <%= @item.price %></h1>
                    <div class="d-flex flex-row user-ratings">
                        <div class="ratings">
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="d-flex shadow rounded justify-content-between align-items-center mt-2 mb-2">

            
        </div>
                
        <button class="btn btn-dark mt-3">Add to cart</button>
    </div>
    
</div>


<% @items.each do |item| %>
    <div class="categories-containers">
    <div class="category-cardd">
    <div class="container">
    <h1><%= item.name%></h1>
    <h1>$<%=item.price%></h1>
    Qty : <%= text_field_tag 'quantity', '1', :size => 2, :maxlength => 2 %>
     <p><%= link_to "Addtocart", cart_items_path(:item_id => item.id),method: :post %> </p> 
    </div>
      </div>
  <% end %>
  